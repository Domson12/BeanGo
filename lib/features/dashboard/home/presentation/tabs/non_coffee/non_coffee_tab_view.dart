import 'package:flutter/material.dart';

class NonCoffeeTabView extends StatelessWidget {
  const NonCoffeeTabView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Center(
        child: Text('Non Coffee Tab View'),
      ),
    );
  }
}
