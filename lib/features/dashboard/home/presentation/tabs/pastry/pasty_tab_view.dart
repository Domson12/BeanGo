import 'package:flutter/material.dart';

class PastyTabView extends StatelessWidget {
  const PastyTabView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Center(
        child: Text('Pasty Tab View'),
      ),
    );
  }
}
